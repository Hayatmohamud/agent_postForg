{"id": "R-0001", "at": "2026-09-16T07:15:00Z", "from": "T01", "type": "info", "severity": "low", "summary": "npx inngest-cli@latest dev fails in this sandboxed shell unless install scripts are explicitly allowed", "details": "The harness blocks npm postinstall scripts by default (visible via 'npm warn install-scripts ... not covered by allowScripts'). inngest-cli's postinstall fetches its platform binary, so a bare `npx inngest-cli@latest dev` fails with 'Error: Inngest CLI binary not found' in this environment even though `INNGEST_DEV=1` and the /api/inngest route are wired correctly. Verified the wiring by temporarily adding inngest-cli as a devDependency, running `npm install-scripts approve inngest-cli` + `npm rebuild inngest-cli`, then running the locally-installed binary directly (./node_modules/.bin/inngest dev -u http://localhost:3000/api/inngest) — this synced the post-forge app with functionCount: 0 as expected. Removed the temporary devDependency afterward so package.json matches the BRD's npx-based workflow.", "affects": ["T16"], "proposedAction": "When T16 (or any human) runs `npx inngest-cli@latest dev` locally outside this sandbox, it should work normally since real environments don't block install scripts. If verifying again inside a similarly sandboxed agent shell, use `npm install -D inngest-cli && npm install-scripts approve inngest-cli && npm rebuild inngest-cli` and run the local binary instead of npx.", "needsHuman": false, "status": "open", "resolution": null}
{"id": "R-0002", "at": "2026-09-16T00:00:00Z", "from": "T02", "type": "blocker", "severity": "high", "summary": "No local MongoDB reachable in this environment to live-verify mongo.ts/posts-repo.ts/settings-repo.ts", "details": "MONGODB_URI is unset (no .env in this worktree) and 127.0.0.1:27017 refuses connection (Test-NetConnection returns false); no mongod/mongosh binary or Windows MongoDB service found either. Built src/lib/mongo.ts (cached client + GridFS bucket + createIndex calls for createdAt/status/topic-text/unique-runId), src/lib/posts-repo.ts (createPost/getPostById/getPostByRunId/listPosts/updateStage/appendSubProgress/setStatus/saveSources/recordTelemetry/recordError), and src/lib/settings-repo.ts (getSettings/putSettings) per the BRD's exact contract, and confirmed via a scratch tsx script that they throw the correct typed MissingEnvError('MONGODB_URI') rather than crashing when unconfigured. Could not exercise the actual GridFS byte round-trip, updateStage scoping, or settings round-trip against a live server.", "affects": ["T03", "T04", "T05", "T06", "T14", "T17"], "proposedAction": "Provide a reachable MONGODB_URI (local mongod install/service, or a dev Atlas URI) and re-run the DoD's Mongo checks (GridFS upload/download byte-equality; createPost->updateStage->getPostById scoping; putSettings/getSettings round-trip) before marking T02 done, or accept code-review-only verification for this part.", "needsHuman": true, "status": "open", "resolution": null}
{"id": "R-0003", "at": "2026-09-16T00:00:00Z", "from": "T02", "type": "blocker", "severity": "high", "summary": "No OPENROUTER_API_KEY available in this environment to live-verify models.ts/image.ts against OpenRouter", "details": "No .env exists in this worktree and OPENROUTER_API_KEY/OPEN_ROUTER are unset in process.env. Built src/lib/models.ts (smartModel()/cheapModel()/gpt5() alias, each openai({model, apiKey: requireOpenRouterKey(), baseUrl: 'https://openrouter.ai/api/v1'}) with override->env-tier->default resolution) and src/lib/image.ts (generatePoster(prompt, modelOverride?) -> {bytes, mime}, typed ImageGenerationError on failure). Verified via a scratch tsx script that calling smartModel()/cheapModel()/gpt5() and generatePoster() with no key raises MissingEnvError / ImageGenerationError respectively (not a crash), and that types/imports compile cleanly and `npm run typecheck`/`npm run build` both pass. Could NOT verify a live text completion via smartModel()/cheapModel(), nor a live generatePoster('a red circle') call against OpenRouter's actual API.", "affects": ["T03", "T04", "T05"], "proposedAction": "Provide a real OPENROUTER_API_KEY (env or .env) and re-run: (1) a trivial completion through smartModel()/cheapModel(), (2) generatePoster('a red circle') expecting {bytes,mime}, before marking T02 fully done, or accept code-review-only verification for this part.", "needsHuman": true, "status": "open", "resolution": null}
{"id": "R-0004", "at": "2026-09-16T00:00:00Z", "from": "T02", "type": "assumption-broken", "severity": "medium", "summary": "OpenRouter's current published docs describe image generation via a dedicated POST /api/v1/images endpoint (model+prompt -> data:[{b64_json,media_type}]), not the chat-completions modalities:[\"image\",\"text\"] shape the BRD guessed", "details": "Web research (OpenRouter's own docs pages under openrouter.ai/docs/guides/overview/multimodal/image-generation and openrouter.ai/docs/api-reference/images/create-images, plus their 'every modality one api' blog post, 2026-09) consistently documents a dedicated `/api/v1/images` endpoint: request {model, prompt, n?, resolution?, aspect_ratio?, quality?, output_format?}, response {created, data:[{b64_json, media_type}], usage}. Some older/third-party sources (blog tutorials, a LobeHub provider integration) instead describe Gemini 'nano banana'-style image models (e.g. gemini-2.5-flash-image) being invoked via POST /chat/completions with modalities:['image','text'], with the image appearing at choices[0].message.images[0].image_url.url as a data: URL. Since no OPENROUTER_API_KEY was available in this environment to test live (see R-0003), I could not confirm which shape google/gemini-3.1-flash-image (the configured IMAGE_MODEL) actually uses today. Implemented src/lib/image.ts defensively: it POSTs to /api/v1/images first (the current, better-documented path) and falls back to parsing the chat-completions modalities shape if /images 404s, so either behavior works without caller changes.", "affects": ["T03"], "proposedAction": "Once a real OPENROUTER_API_KEY is available, run generatePoster('a red circle') live and confirm which code path actually returns the image for google/gemini-3.1-flash-image; delete the unused path in image.ts to simplify if only one is ever needed.", "needsHuman": false, "status": "open", "resolution": null}
{"id": "R-0005", "at": "2026-09-16T11:20:00Z", "from": "T07", "type": "assumption-broken", "severity": "low", "summary": "Gallery route moved from _gallery to gallery: underscore-prefixed folders are excluded from Next.js App Router routing entirely", "details": "The T07 brief specified the component gallery at src/app/(app)/_gallery/page.tsx. Confirmed against current Next.js docs (Context7) that any path segment starting with `_` is a 'private folder' and is unconditionally filtered out of route discovery (ignorePartFilter: part => part.startsWith('_')) -- so /_gallery would 404 no matter what, both in `next build` output and at runtime. Built the page at src/app/(app)/gallery/page.tsx instead (route: /gallery). Verified: `npm run build` lists '/gallery' as a static route, and `npm run dev` + curl returns 200 with the expected page markup and no server-side error/compile warnings.", "affects": ["T08", "T09", "T10", "T11", "T12"], "proposedAction": "Any later task/BRD or docs referencing the gallery path should use src/app/(app)/gallery/page.tsx (route /gallery), not _gallery.", "needsHuman": false, "status": "open", "resolution": null}
{"id": "R-0006", "at": "2026-09-16T11:40:00Z", "from": "T09", "type": "info", "severity": "low", "summary": "Used a broad 'Stop-Process -Force -Name node' to shut down my own dev server; this could have killed other parallel worktrees' node processes on this shared Windows machine", "details": "After curling / , /sign-in, /sign-up against my `npm run dev` instance to verify 200s and clean server logs, I stopped it with `powershell -Command \"Get-Process -Name node | Stop-Process -Force\"` instead of targeting only my own process/port. On a machine running multiple parallel task worktrees (T03/T08/etc. per PLAN.md wave 2), this command is not scoped and could terminate any other agent's currently-running `next dev`/`npm install`/build process. I did not observe direct evidence of harm (my own build/typecheck re-ran clean afterward), but I have no visibility into other worktrees' sessions to confirm they were unaffected.", "affects": ["T03", "T08"], "proposedAction": "If another wave-2 task's dev server or long-running npm process died unexpectedly around 2026-09-16T11:3x-11:4x, this is the likely cause — just restart it. Future agents on shared machines should kill by PID/port (e.g. filter Get-Process by CommandLine or use the port) rather than by process name.", "needsHuman": false, "status": "open", "resolution": null}
{"id":"R-0007","at":"2026-09-16T08:39:09.313Z","from":"T03","type":"blocker","severity":"medium","summary":"No SERPER_API_KEY, OPENROUTER_API_KEY, or reachable MongoDB in this worktree to live-verify web_search/generate_poster/save_post end-to-end","details":"No .env exists in wt-T03/post-forge and none of SERPER_API_KEY, OPENROUTER_API_KEY/OPEN_ROUTER, or MONGODB_URI are set in process.env; 127.0.0.1:27017 is unreachable (same environment gap already flagged by T02 in R-0002/R-0003). Built all four tools (web_search.ts, fetch_url.ts, generate_poster.ts, save_post.ts) per the BRD, including the SSRF guard and the imageModel (not imageProvider) plumbing from the amendment. Live-verified everything NOT blocked by missing keys via a scratch tsx script exercising each tool.handler() directly with a fake {network,step} context: (1) fetch_url's SSRF guard blocks http://169.254.169.254/ (cloud metadata), http://localhost:9999/, http://127.0.0.1/, http://192.168.1.1/, and file:// scheme -- all throw SsrfBlockedError; (2) fetch_url successfully fetched and readability-extracted https://en.wikipedia.org/wiki/2026_FIFA_World_Cup to 10000 chars (capped, truncated:true); (3) fetch_url degrades gracefully (ok:false, no throw) on a non-HTML URL (https://httpbin.org/json); (4) web_search throws a clean WebSearchError naming the missing key when SERPER_API_KEY is unset; (5) generate_poster throws a clean GeneratePosterToolError wrapping ImageGenerationError(missing OPENROUTER_API_KEY) when unset; (6) save_post throws a clean SavePostToolError wrapping MissingEnvError(MONGODB_URI) when unset. Could NOT verify: a live Serper query returning structured {title,url,snippet}[], a live generate_poster GridFS round-trip (bytes+metadata retrievable), or save_post's idempotent-upsert-by-runId behavior against a real Mongo instance.","affects":["T04","T15","T17"],"proposedAction":"Once a real SERPER_API_KEY, OPENROUTER_API_KEY, and reachable MONGODB_URI are available (the same env gap as R-0002/R-0003), re-run: web_search({query:...}) expecting structured results; generate_poster({prompt:...}) then fetch the returned id back from the posters GridFS bucket and confirm bytes + {mime,postId} metadata; save_post(...) called twice with the same runId and confirm exactly one Post document (idempotent). All handler code paths are implemented and typecheck/build clean; this is purely an environment-verification gap, not a known code defect.","needsHuman":true,"status":"open","resolution":null}
{"id":"R-0008","at":"2026-09-16T09:02:34.300Z","from":"T17","type":"new-requirement","severity":"low","summary":"T17 seeded a schedules collection ahead of T14; defined a Schedule shape for T14 to adopt","details":"T14 (Scheduled/cron management) has not been built yet, but T17's BRD asks for sample schedules. Defined this Schedule type directly in post-forge/scripts/seed.ts (no new src/lib/schedules-repo.ts, to avoid pre-empting T14's own file layout): { _id: ObjectId; topic: string; cadence: \"daily\" | \"weekly\"; enabled: boolean; nextRunAt: Date; lastResult?: { status: \"success\" | \"failure\"; at: Date; postId?: string }; createdAt: Date; updatedAt: Date }. Collection name is \"schedules\". Seeded 2 fixed-id docs directly via getDb().collection(\"schedules\"): one enabled/daily with a lastResult referencing a seeded done post, one disabled/weekly with no lastResult. Also confirmed the same MongoDB-unreachable environment gap already flagged in R-0002 (127.0.0.1:27017 ECONNREFUSED, no .env/MONGODB_URI) blocks live verification of the seed script here too -- not filing a duplicate blocker for that, just noting it against R-0002.","affects":["T14"],"proposedAction":"When T14 is implemented, adopt this Schedule shape (or explicitly amend it here) rather than inventing a conflicting one, since seed.ts and any future schedules-repo.ts / API routes need to agree on the same document contract. If T14 changes the shape, update scripts/seed.ts to match so the seed data stays representative.","needsHuman":false,"status":"open","resolution":null}
{"id":"R-0009","at":"2026-09-16T09:05:26.000Z","from":"T04","type":"blocker","severity":"medium","summary":"No OPENROUTER_API_KEY/SERPER_API_KEY/MONGODB_URI in this worktree to live-verify buildNetwork(options).run(topic) end-to-end","details":"Same environment gap already flagged by T02 (R-0002/R-0003) and T03 (R-0007): no .env exists in wt-T04/post-forge and none of OPENROUTER_API_KEY/OPEN_ROUTER, SERPER_API_KEY, MONGODB_URI are set in process.env. Built all six agents (src/agents/research.ts, verify.ts, writer.ts, editor.ts, illustrator.ts, publisher.ts) per the BRD's per-agent model tiers (SMART=verify+editor via smartModel(), CHEAP=research+writer+illustrator+publisher via cheapModel()) plus src/agents/network.ts (buildNetwork(options) factory + createNetwork + the deterministic if-ladder router, exported standalone as routeNext for isolated testing) and src/agents/shared.ts (stripUnknownToolCalls onResponse guard against AgentKit's 'Inference requested a non-existent tool' throw; a per-run/per-stage WeakMap attempt counter used to force a best-effort degraded fallback after MAX_STAGE_ATTEMPTS=3 instead of spinning to the network's hard maxIter; best-effort tryParseJson). Fully verified router determinism in isolation with zero external services: added src/agents/network.router.test.ts (vitest) asserting routeNext returns the correct next agent for all 7 BRD-required partial states (empty/research-only/+verifiedFindings/+draft/+finalPost/+posterImageId/+published->undefined) plus 2 edge cases (empty-array research/verifiedFindings treated as not-done) -- all 9 pass. Also added a minimal vitest.config.ts (none existed) purely to resolve the '@/*' tsconfig path alias for this test; T18 should own/extend it. `npm run typecheck` and `npm run build` (webpack) both pass. Could NOT live-run buildNetwork(options).run('some topic') end-to-end (no model calls, no live web_search, no Mongo persistence possible), so the six-stage live pipeline, the verify agent's actual drop/flag behavior against real search results, and tone/length affecting real model output are unverified beyond code review.","affects":["T05","T18"],"proposedAction":"Once a real OPENROUTER_API_KEY (+ ideally SERPER_API_KEY and a reachable MONGODB_URI) are available, run buildNetwork({runId, options}).run('World Cup 2026') and confirm: all six stages fire in order; verifiedFindings carries real verified booleans reflecting an actual re-check; finalPost/title/posterImageId/published all end up set; options.tone='witty'/length='short' visibly changes the draft. This is purely an environment-verification gap (same root cause as R-0002/R-0003/R-0007), not a known code defect.","needsHuman":true,"status":"open","resolution":null}
{"id":"R-0010","at":"2026-09-16T09:40:00.000Z","from":"T05","type":"blocker","severity":"medium","summary":"No OPENROUTER_API_KEY/SERPER_API_KEY/reachable MONGODB_URI in this worktree to live-verify generatePost end-to-end (same root cause as R-0002/R-0003/R-0007/R-0009)","details":"No .env exists in wt-T05/post-forge; OPENROUTER_API_KEY, SERPER_API_KEY, MONGODB_URI all unset. Built src/inngest/functions.ts (generatePost) and registered it in src/app/api/inngest/route.ts per the BRD. npm run typecheck and npm run build (webpack) both pass. Live-verified everything NOT gated by real credentials: started `npm run dev` (INNGEST_DEV=1) and a local `inngest dev` binary (installed as a temporary devDependency + `npm install-scripts approve inngest-cli` + `npm rebuild`, then uninstalled afterward, per R-0001's documented workaround for this sandbox's blocked postinstall scripts) pointed at http://localhost:3000/api/inngest. Confirmed via GET /api/inngest (function_count:1) and the dev server's GraphQL API (`{ functions { name slug } }` -> generate-post / post-forge-generate-post) that the function is discovered and synced. Posted a real post/generate.requested event ({postId,runId,topic,options}) via POST http://localhost:8288/e/test-key and confirmed in both the Next.js dev log and the inngest dev log that: the event was received, the function initialized, the `load-post` step ran and called getPostById -> postsCollection -> getDb -> requireEnv('MONGODB_URI'), which threw the expected MissingEnvError (not a NonRetriableError), and Inngest's own retry/backoff mechanism picked it up as expected. This confirms the event contract, function registration, and the load-post step's error path are wired correctly end-to-end. Could NOT verify: an actual six-stage pipeline run (needs a real Mongo doc + OPENROUTER_API_KEY + SERPER_API_KEY), per-stage Mongo persistence writes actually landing (updateStage/appendSubProgress/recordTelemetry), the withoutInngestStepContext() ALS-exit path actually preventing an OpenRouter step.ai.infer 400 (this is the single highest-value thing to re-verify live once credentials exist -- see the design-decision doc-comment at the top of functions.ts), or the terminal failure path (bad SERPER_API_KEY -> retries -> status:failed with partial stages intact).","affects":["T06","T11","T14","T18"],"proposedAction":"Once a real OPENROUTER_API_KEY + SERPER_API_KEY + reachable MONGODB_URI exist, and once T06 exists to create a real post doc (or a doc is inserted manually with a matching runId), emit post/generate.requested and confirm: (1) the doc's status advances researching->verifying->writing->editing->illustrating->publishing->done with stages/subProgress/telemetry populated incrementally (poll getPostById mid-run); (2) each of the six agents actually fires and completes; (3) no OpenRouter/step.ai.infer 400 occurs (this is the specific, previously-hit failure mode the withoutInngestStepContext() helper in functions.ts exists to avoid -- if it DOES still occur, the ALS symbol lookup in that helper is the first place to check, since it depends on Inngest's internal Symbol.for('inngest:als') implementation detail rather than a stable public API); (4) a temporarily-bad SERPER_API_KEY still results in a completed run with degraded content (per R-0011) rather than a genuine failure, confirming that finding; (5) a temporarily-bad OPENROUTER_API_KEY or unreachable Mongo produces an actual status:failed doc with a populated error field and partial stages intact. This is the highest-value task in the whole project to re-verify live once credentials exist, since it is the only one exercising the full durable pipeline.","needsHuman":true,"status":"open","resolution":null}
{"id":"R-0011","at":"2026-09-16T09:42:00.000Z","from":"T05","type":"assumption-broken","severity":"medium","summary":"T04's agents (already merged) swallow essentially all tool/model errors into graceful-degradation fallbacks, so T05's BRD acceptance criterion 'a bad SERPER_API_KEY triggers retries then status:failed' will likely never actually fire that way","details":"Read src/agents/research.ts, verify.ts, illustrator.ts, publisher.ts, shared.ts and AgentKit's invokeTools() in node_modules/@inngest/agent-kit/dist/chunk-BSWKEFTT.js closely while designing T05's error/retry handling. Two independent layers already catch tool errors before they can ever reach network.run()'s caller: (1) AgentKit's own invokeTools() wraps every tool handler call in `.catch(err => ({error: serializeError(err)}))`, turning a thrown WebSearchError/ImageGenerationError/etc. into a tool_result message fed back to the model, never an exception; (2) each T04 agent's own onFinish lifecycle hook has a MAX_STAGE_ATTEMPTS-bounded graceful-degradation fallback (e.g. research.ts's fallbackFindingsFromToolCalls, verify.ts passing research through unverified, illustrator.ts/publisher.ts calling the tool handler directly with a deterministic fallback prompt/args) that is specifically designed to let the stage complete with degraded content rather than stall or throw. Additionally, confirmed by reading agent-kit's network execute loop (chunk-BSWKEFTT.js) that hitting the network's own maxIter safety cap (MAX_ROUTER_ITERATIONS=24, agents/shared.ts) does NOT throw either -- the while loop just exits, so network.run() resolves normally even for a pipeline that never completes. Net effect: a bad SERPER_API_KEY will very likely just produce a low-quality-but-complete post (status:done, with placeholder/degraded research and verifiedFindings) rather than the BRD's acceptance-criterion #3 scenario ('injecting a failing tool triggers retries, then the doc goes status:failed'). T05's generatePost (src/inngest/functions.ts) is written to be correct for the failure modes that CAN actually occur (a hard throw from a real network/DB/model-plumbing failure, e.g. bad MONGODB_URI or a malformed OpenRouter response, or the pipeline silently stalling short of maxIter without publishing -- which functions.ts detects post-run by checking status !== 'done' and treats as failed) but cannot make a merged, out-of-scope T04 file (agents/*.ts) rethrow tool errors instead of degrading them.","affects":["T04","T06","T11","T16","T18"],"proposedAction":"Decide, with a human or the orchestrator, whether this is actually the desired behavior (graceful degradation over hard failure is arguably the better UX for a bad search-API key specifically) or whether T04's tools/agents should be amended to let certain error classes (e.g. WebSearchError, a 401/403 from OpenRouter) propagate as real exceptions instead of degrading, so T16's end-to-end 'bad SERPER_API_KEY -> status:failed' verification step actually exercises the failure path it describes. If degradation is kept as-is, T16's verification script for that acceptance criterion should be amended to expect a completed-but-degraded post instead of a failed one, and a different fault (e.g. a bad MONGODB_URI, or a bad OPENROUTER_API_KEY causing a genuine unhandled exception) should be used to demonstrate the actual status:failed path this task (T05) implements.","needsHuman":true,"status":"open","resolution":null}
{"id":"R-0012","at":"2026-09-17T08:20:00.000Z","from":"T06","type":"blocker","severity":"medium","summary":"No reachable MongoDB (and no OPENROUTER_API_KEY/SERPER_API_KEY) in this worktree to live-verify the API surface against real data; only .env.example exists","details":"Same root-cause environment gap already flagged in R-0002/R-0003/R-0007/R-0009/R-0010: no .env exists in wt-T06/post-forge, MONGODB_URI/OPENROUTER_API_KEY/SERPER_API_KEY are all unset in process.env, and 127.0.0.1:27017 refuses connection (Test-NetConnection returns false), so `npm run seed` and every route's actual Mongo/GridFS read/write path could not be exercised end-to-end. Built all six routes + src/lib/dto.ts per the BRD: POST /api/generate (src/app/api/generate/core.ts + route.ts, split per the known Next.js route-export-validation pitfall), GET /api/posts (list w/ search/status/from/to/page/pageSize), GET+DELETE /api/posts/[id] (poll shape + GridFS poster cleanup), GET /api/posters/[id] (GridFS byte stream w/ Content-Type from metadata.mime + immutable Cache-Control, clean 404), GET /api/stats (in-JS aggregation: totals, avg generation/stage timings, tokensByAgent, activityByDay), GET/PUT /api/settings. `npm run typecheck` and `npm run build` (webpack) both pass; all 7 API routes appear in the build's route list. Live-verified everything NOT gated by Mongo: started `npm run dev` (a server was already running on :3000 from an earlier background attempt) and curled every route with no MONGODB_URI configured -- confirmed each one fails gracefully with the standard `{error:{message,code}}` shape and a clean 500 (never a raw stack trace or crash): GET /api/posts, GET /api/settings, GET /api/stats, POST /api/generate (valid body) all returned `MissingEnvError: MONGODB_URI is not set` wrapped in the standard error shape. Also confirmed the validation/routing logic that does NOT need Mongo works exactly as specified: GET /api/posts/notanid -> 400 invalid_id (ObjectId.isValid check, no DB call); GET /api/posters/notanid -> 404 not_found (same, no DB call); POST /api/generate with {} -> 400 invalid_request with a readable zod message (topic required). Could NOT verify: an actual `npm run seed` + GET /api/posts search/status/pagination against real seeded docs, GET /api/posts/[id] poll shape against a real seeded Post, DELETE actually removing both the post doc and its GridFS poster bytes, GET /api/posters/[id] streaming real bytes with the correct content-type, GET/PUT /api/settings round-tripping against a real settings doc, GET /api/stats returning sane numbers against known seed data, or POST /api/generate's full doc-creation + inngest.send() path (verified only by code review -- it calls createPost(topic, runId, options) then inngest.send({name:'post/generate.requested', data:{postId, runId, topic, options}}) exactly per the BRD and T05's event contract).","affects":["T10","T11","T12","T13","T15","T18","T19"],"proposedAction":"Once a reachable MONGODB_URI exists (env or .env, same gap as R-0002 etc.), run `npm run seed` then re-verify per the DoD: GET /api/posts?search=...&status=done&page=1 against seeded docs; GET /api/posts/[id] on a seeded done id (full payload present) and the seeded in-progress id (poll shape only); DELETE /api/posts/[id] then confirm GET /api/posters/[posterImageId] now 404s; GET /api/posters/[id] on a seeded poster id returns the right bytes+mime; PUT /api/settings {tone:'witty'} then GET round-trips it; GET /api/stats returns numbers consistent with the 4 seeded posts (1 failed, 2 done, 1 in-progress). Also worth doing once OPENROUTER_API_KEY exists: a full POST /api/generate -> poll /api/posts/[id] -> done flow, confirming the doc T06 creates is the same one T05's generatePost function updates by postId.","needsHuman":true,"status":"open","resolution":null}
