{"id": "R-0001", "at": "2026-09-16T07:15:00Z", "from": "T01", "type": "info", "severity": "low", "summary": "npx inngest-cli@latest dev fails in this sandboxed shell unless install scripts are explicitly allowed", "details": "The harness blocks npm postinstall scripts by default (visible via 'npm warn install-scripts ... not covered by allowScripts'). inngest-cli's postinstall fetches its platform binary, so a bare `npx inngest-cli@latest dev` fails with 'Error: Inngest CLI binary not found' in this environment even though `INNGEST_DEV=1` and the /api/inngest route are wired correctly. Verified the wiring by temporarily adding inngest-cli as a devDependency, running `npm install-scripts approve inngest-cli` + `npm rebuild inngest-cli`, then running the locally-installed binary directly (./node_modules/.bin/inngest dev -u http://localhost:3000/api/inngest) — this synced the post-forge app with functionCount: 0 as expected. Removed the temporary devDependency afterward so package.json matches the BRD's npx-based workflow.", "affects": ["T16"], "proposedAction": "When T16 (or any human) runs `npx inngest-cli@latest dev` locally outside this sandbox, it should work normally since real environments don't block install scripts. If verifying again inside a similarly sandboxed agent shell, use `npm install -D inngest-cli && npm install-scripts approve inngest-cli && npm rebuild inngest-cli` and run the local binary instead of npx.", "needsHuman": false, "status": "open", "resolution": null}
{"id": "R-0005", "at": "2026-09-16T11:20:00Z", "from": "T07", "type": "assumption-broken", "severity": "low", "summary": "Gallery route moved from _gallery to gallery: underscore-prefixed folders are excluded from Next.js App Router routing entirely", "details": "The T07 brief specified the component gallery at src/app/(app)/_gallery/page.tsx. Confirmed against current Next.js docs (Context7) that any path segment starting with `_` is a 'private folder' and is unconditionally filtered out of route discovery (ignorePartFilter: part => part.startsWith('_')) -- so /_gallery would 404 no matter what, both in `next build` output and at runtime. Built the page at src/app/(app)/gallery/page.tsx instead (route: /gallery). Verified: `npm run build` lists '/gallery' as a static route, and `npm run dev` + curl returns 200 with the expected page markup and no server-side error/compile warnings.", "affects": ["T08", "T09", "T10", "T11", "T12"], "proposedAction": "Any later task/BRD or docs referencing the gallery path should use src/app/(app)/gallery/page.tsx (route /gallery), not _gallery.", "needsHuman": false, "status": "open", "resolution": null}
